class Picture {
  constructor(img, x, y, w, h) {
    this.img = img; // Изображение
    this.x = x; // Координата X
    this.y = y; // Координата Y
    this.w = w; // Ширина
    this.h = h; // Высота
  }
}
