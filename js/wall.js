class Wall {
  constructor(x, y, w, h) {
    this.x = x; // Координата x стены
    this.y = y; // Координата y стены
    this.w = w; // Ширина стены
    this.h = h; // Высота стены
    this.typ = "wall"; // Тип объекта - стена
  }
}
